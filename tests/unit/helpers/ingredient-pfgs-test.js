import { ingredientPfgs } from '../../../helpers/ingredient-pfgs';
import { module, test } from 'qunit';

module('Unit | Helper | ingredient pfgs');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = ingredientPfgs(42);
  assert.ok(result);
});
