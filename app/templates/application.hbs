<div class="container">
  <nav class="navbar navbar-default">
    <div class="container-fluid">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        {{#link-to 'index' class="navbar-brand"}}High West{{/link-to}}
      </div>

      <div class="collaps navbar-collapse">
        <ul class="nav navbar-nav">
          {{!-- {{#if session.isAuthenticated}} --}}
          <li>
            {{#link-to 'recipes'}}Recipes{{/link-to}}
          </li>
          <li>
            {{#link-to 'spiritTypes'}}Spirit types{{/link-to}}
          </li>
          <li>
            {{#link-to 'locations'}}Locations{{/link-to}}
          </li>
          {{!-- {{/if}} --}}
        </ul>
        {{!-- <ul class="nav navbar-nav navbar-right">
          {{#if session.isAuthenticated}}
          <li>
            <button class="btn btn-default navbar-btn" {{ action 'invalidateSession' }}>
              Logout
            </button>
          </li>
          {{else}}
          <li>
            {{#link-to 'login' class="btn btn-primary navbar-btn" tagName="button"}}Login{{/link-to}}
          </li>
          {{/if}}
        </ul> --}}
      </div>
    </div>
  </nav>

  {{outlet}}
</div>
